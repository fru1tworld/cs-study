# Gradle 튜토리얼 6장 - 빌드 캐시 활성화하기

> **원문:** https://docs.gradle.org/current/userguide/part6_gradle_caching.html

## 이 장에서 다루는 것

이전 장(5장)에서 다룬 증분 빌드는 "같은 워크스페이스에서 방금 전 실행"과 비교해 변경이 없으면 태스크를 건너뛰는 최적화였다. 하지만 브랜치를 전환하거나 다른 사람이 같은 코드를 빌드하는 상황에서는 그 이력이 무의미해져 매번 처음부터 다시 빌드해야 한다. 이 장은 이 문제를 **빌드 캐시(Build Cache)**로 해결하는 방법을 다룬다.

- 로컬 빌드 캐시를 켜고 동작을 확인한다
- 브랜치 전환 같은 상황을 흉내 내며 캐시 재사용을 관찰한다
- 원격 빌드 캐시로 여러 사람/CI가 결과를 공유하는 개념을 익힌다

## 왜 캐시가 필요한가

증분 빌드는 "직전 실행"만 기억한다. 그래서 다음과 같은 상황에서는 무력하다.

- 브랜치를 A에서 B로 옮긴 뒤 다시 A로 돌아왔을 때
- 동료가 이미 컴파일해본 소스를 내 로컬에서 다시 컴파일할 때
- CI가 같은 커밋을 여러 파이프라인에서 반복 빌드할 때

이런 경우에도 태스크의 **입력이 동일하다면 출력도 동일**하다는 사실은 변하지 않는다. 빌드 캐시는 이 점을 이용해, 한 번 만든 결과물을 어딘가에 저장해두고 입력이 같은 태스크를 만나면 실행 대신 결과물을 그대로 복사해온다.

## 로컬 빌드 캐시 켜기

빌드 캐시는 기본적으로 꺼져 있다. `gradle.properties`에 한 줄만 추가하면 프로젝트 전역에서 활성화할 수 있다.

```properties
org.gradle.caching=true
```

옵션 없이 매번 켜고 끄고 싶다면 커맨드라인 플래그로도 지정할 수 있다.

```bash
./gradlew :app:build --build-cache
```

## 캐시 재사용 확인하기 (브랜치 전환 흉내)

캐시 효과를 눈으로 보려면 먼저 정상적으로 한 번 빌드하고, 그다음 `clean`으로 빌드 결과물만 지운 뒤 다시 빌드해본다. 이는 "다른 브랜치로 갔다가 돌아온" 상황과 유사하다.

```bash
./gradlew :app:build          # 1차 빌드 - 태스크 실행, 캐시에 결과 저장
./gradlew :app:clean :app:build   # 출력물만 삭제 후 재빌드
```

두 번째 빌드의 결과 라벨을 보면 캐시가 실제로 무엇을 했는지 구분할 수 있다.

| 라벨 | 의미 |
| --- | --- |
| `UP-TO-DATE` | 로컬 실행 이력이 남아있어 증분 빌드가 건너뜀 |
| `FROM-CACHE` | `clean`으로 이력은 지워졌지만, 빌드 캐시에 저장된 출력물을 그대로 복사해옴 |

즉 `clean`은 로컬의 "실행 이력과 출력 파일"만 지울 뿐, 별도로 보관된 캐시 저장소는 건드리지 않는다. 그래서 `FROM-CACHE`가 나타나는 것은 "태스크를 다시 안 돌리고 캐시에서 결과만 복사했다"는 신호다.

**핵심 포인트**
- 캐시 히트 시 Gradle은 태스크를 실행하는 대신 저장된 출력을 프로젝트 빌드 디렉터리로 복사해 넣는다.
- `UP-TO-DATE`는 "이력이 남아있어 아예 비교할 필요조차 없었다"는 뜻이고, `FROM-CACHE`는 "이력은 없어졌지만 저장된 산출물을 재사용했다"는 뜻으로, 서로 다른 최적화 경로다.

## 로컬 캐시는 어디에 저장되나

로컬 빌드 캐시는 사용자 홈 디렉터리 아래 Gradle 캐시 폴더에 저장된다.

- Windows: `%USERPROFILE%\.gradle\caches`
- macOS/Linux: `~/.gradle/caches/`

이 저장소는 무한정 커지지 않는다. Gradle이 오래 쓰이지 않은 항목을 자동으로 정리해 크기를 관리한다.

## 원격 빌드 캐시 - 팀/CI 간 공유

로컬 캐시는 "내 컴퓨터 안에서의 재사용"에 그친다. 여러 개발자와 CI 서버가 같은 결과물을 공유하려면 **원격 빌드 캐시**가 필요하다.

캐시를 찾는 순서는 다음과 같다.

1. 로컬 캐시를 먼저 확인한다
2. 로컬에 없으면 원격 캐시에서 다운로드를 시도한다
3. 어디에도 없으면 태스크를 실제로 실행한다

Gradle은 테스트/학습용으로 무료 Docker 이미지를 제공하며, 실제 팀/프로덕션 환경에서는 Gradle에서 만든 상용 솔루션인 **Develocity**를 통한 원격 캐시 구축을 권장한다.

**핵심 포인트**
- 원격 캐시의 가치는 "같은 입력을 처음 만나는 사람"에게는 없고, "누군가 이미 겪은 입력을 다시 만나는 사람/CI"에게 크다. 팀 규모와 빌드 빈도가 클수록 효과가 커진다.
- 로컬 캐시와 원격 캐시는 배타적이지 않고 계층적으로 함께 동작한다.

## 이번 장 정리

| 개념 | 범위 | 활성화 방법 | 결과 라벨 |
| --- | --- | --- | --- |
| 증분 빌드 (5장) | 같은 워크스페이스, 직전 실행 | 항상 켜짐 | `UP-TO-DATE` |
| 로컬 빌드 캐시 | 내 컴퓨터, 여러 이력 중 재사용 | `org.gradle.caching=true` | `FROM-CACHE` |
| 원격 빌드 캐시 | 팀원/CI 간 공유 | 별도 캐시 서버(Docker, Develocity 등) 연결 | `FROM-CACHE` |

다음 단계로는 지금까지의 초급 튜토리얼(초기화, 태스크 실행, 의존성 관리, 플러그인, 증분 빌드, 빌드 캐시)을 복습하거나, 중급 튜토리얼로 넘어가는 것을 권장한다.
